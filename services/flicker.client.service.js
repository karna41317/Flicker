'use strict';

//Flicker service used for communicating with the articles REST endpoints
angular.module('flicker').factory('Flicker', ['$resource',
	function($resource) {
		//return $resource();	
	}
]);